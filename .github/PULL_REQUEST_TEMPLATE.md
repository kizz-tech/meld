## What

Brief description of the change.

## Why

What problem does this solve? Link to issue if applicable (e.g., Fixes #123).

## How

Key implementation details (if non-obvious).

## Checklist

- [ ] Only related files are included (no IDE configs, generated files, or local artifacts)
- [ ] Commit messages follow [Conventional Commits](https://www.conventionalcommits.org/) (`feat:`, `fix:`, `docs:`, etc.)
- [ ] `cargo clippy -- -D warnings` passes
- [ ] `cargo test` passes
- [ ] `pnpm lint` passes
- [ ] Manually tested the change
